import { Knex } from "knex";

declare module 'Knex/types/tables' {
    export interface Tables {
        /*users: {
            id: string,
            name: string,
            created_at: string
        }
        */
    }
}